/**
 * AI comment markers have been removed — the agent must never inject
 * timestamp comments into files it writes or edits.
 *
 * This file is kept as a no-op stub for backward compatibility.
 */

export function getAICommentMarker(_ext: string, _description?: string): string {
  return '';
}
